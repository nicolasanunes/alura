git init (transforma a pasta local em um projeto .git)
git remote 
    git remote add origin https://github.com/user/repo.git
git clone (copiar o repositório do GitHub para o local)
git log (mostrar logs de alterações dos arquivos)
    git log --author="user_name"
    git log --since=1.month.ago --until=1.day.ago
git log --oneline (mostrar logs de alterações dos arquivos em uma linha)
git pull (atualiza os arquivos do repositório no local)
git status (visualiza o que foi alterado no arquivo)
git commit
    git commit nomedoarquivo -m "mensagem"
git push origin nomedabranch (envia as alterações para o repositório na web)
git pull
git restore --source xxxxxx . (colocar o código do hash gerado no git log --oneline)
git checkout -b nomedabranch
git switch nomedabranch
git branch (mostra as branchs)

MERGE (passar de uma branch para outra)
git merge nomedabranch (que irá para a atual)

