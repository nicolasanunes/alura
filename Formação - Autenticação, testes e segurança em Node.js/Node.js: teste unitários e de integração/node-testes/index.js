const sumExtraHours = (salary, valueExtraHours) => salary + valueExtraHours;

const calculateDiscounts = (salary, discounts) => salary - discounts;

export {
  sumExtraHours,
  calculateDiscounts,
};
